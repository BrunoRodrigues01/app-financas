name: Build iOS

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # Permite executar manualmente

jobs:
  build-ios:
    runs-on: macos-latest  # Usa máquina macOS gratuita do GitHub
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.0'
        channel: 'stable'
    
    - name: Install dependencies
      run: flutter pub get
    
    - name: Analyze code
      run: flutter analyze
    
    - name: Run tests
      run: flutter test
    
    - name: Build iOS (sem assinatura)
      run: |
        flutter build ios --release --no-codesign
    
    - name: Create IPA (Archive)
      run: |
        cd build/ios/iphoneos
        mkdir Payload
        cp -r Runner.app Payload/
        zip -r app-release.ipa Payload
        
    - name: Upload IPA artifact
      uses: actions/upload-artifact@v4
      with:
        name: ios-release
        path: build/ios/iphoneos/app-release.ipa
        retention-days: 30

    - name: Build Info
      run: |
        echo "✅ Build iOS concluído com sucesso!"
        echo "📱 Baixe o arquivo .ipa nos artifacts desta execução"
        echo "⚠️  Nota: Este IPA não está assinado. Para instalar no iPhone:"
        echo "   1. Use Xcode para assinar localmente, OU"
        echo "   2. Configure certificados nos secrets do GitHub"
